{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Minimum permissions for the BX SSH main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
